mixin alert-email-pass-error
	.alert-email-pass-error
		.alert-wrapper
			p Введите верные данные для входа или 
				a(href="#") воспользуйтесь восстановлением пароля, 
				| чтобы войти на сайт.
mixin alert-email-error
	.alert-email-error
		.alert-wrapper
			p Используйте другой email чтобы создать новый аккаунт.
			p Или воспользуйтесь 
				a(href="#") восстановлением пароля,
				| чтобы войти на сайт.

mixin alert(text)
		if block
			div(class!=attributes.class).alert= text
			block
		else
			div(class!=attributes.class).alert= text